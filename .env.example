# V0.1 requires NO secrets and NO API keys. This file exists to document that,
# and to establish where credentials will live later (Section 22: never commit
# keys, never expose them in logs, prompts, reports or browser bundles).
#
# Nothing below is read by V0.1 code.

# --- later phases only ---------------------------------------------------
# MAYSANI_DATA_PROVIDER_KEY=
# MAYSANI_BROKER_API_KEY=          # trading-only credential, never withdrawal
# MAYSANI_BROKER_API_SECRET=
# MAYSANI_BROKER_ENV=paper         # paper | shadow | live
# MAYSANI_DB_URL=
